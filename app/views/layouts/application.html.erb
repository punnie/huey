<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Huey</title>
        <%= yield :head %>
        <%= stylesheet_link_tag "application" %>
        <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= stylesheet_link_tag "application" %>
    </head>
    <body>
        <header class="text-gray-600 body-font sticky top-0 left-0 bg-white border-b-1 border-solid border-gray-100">
            <div class="container mx-auto flex flex-wrap p-2 flex-row items-center">
                <nav class="flex w-1/5 flex-nowrap items-center text-base ml-auto">
                    <% if params['controller'] != 'welcome' %>
                        <a class="text-gray-300" href="<%= root_url %>">Back</a>
                    <% end %>
                </nav>
                <a class="flex w-3/5 items-center items-center justify-center">
                    <span><%= yield :title %></span>
                </a>
                <div class="w-1/5 inline-flex justify-end font-bold text-gray-900">
                    <span>Huey</span>
                </div>
            </div>
        </header>

        <div class="container mx-auto">
            <%= yield %>
        </div>
    </body>
</html>
