<div class="flex flex-wrap">
    <% @feeds.order(:title).each do |feed| %>
        <div class="lg:basis-1/3 w-full">
            <%= render partial: 'shared/feed', locals: { feed: feed, entries: feed.entries.for_feed.limit(10), display_mode: :compact } %>
        </div>
    <% end %>
</div>
